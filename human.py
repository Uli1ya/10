from abc import ABC, abstractmethod


class Human:
    _ids = set()


def __init__(self, name, last_name, __id=None):
    self.name = name
    self.last_name = last_name


def __id(self):
    if id is not None:
        for i in range(_ids):
            if (id is not _ids):
                return id
            else:
                raise Exception("id уже существует")
    else:
        return max(id) + 1  # сгенерировать


@abstractmethod


def set_class(self):
    pass


def get_class(self):
    pass
